DATABASE_URL=postgresql://postgres:postgres@localhost:5433/carbon_analyst

COHERE_API_KEY=
LLM_MODEL=command-a-03-2025
LLM_MAX_TOKENS=4096
VECTOR_DIMENSION=1536
EMBEDDING_MODEL=embed-v4.0
RERANK_MODEL=rerank-v3.5

CLASSIFIER_BACKEND=anthropic
CLASSIFY_CONCURRENCY=5

# Quote Chat (hỏi đáp đoạn bôi đen trong báo cáo) — mặc định "anthropic" (cần
# ANTHROPIC_API_KEY) để dùng được server tool web_search; đổi "cohere" nếu muốn
# dùng Cohere thay thế (khi đó KHÔNG có web_search — xem services/quote_chat.py).
QUOTE_CHAT_BACKEND=anthropic
QUOTE_CHAT_MODEL=claude-haiku-4-5

ANTHROPIC_API_KEY=
EIA_API_KEY=

# --- Auth (admin panel + user login) ---
# Chuỗi random bất kỳ dùng để ký JWT session, vd: python -c "import secrets; print(secrets.token_urlsafe(48))"
JWT_SECRET=
# Đặt tên đăng nhập admin tuỳ ý, và chạy `python -m scripts.generate_admin_hash`
# để lấy giá trị dán vào ADMIN_PASSWORD_HASH bên dưới.
ADMIN_USERNAME=admin
ADMIN_PASSWORD_HASH=
# true khi deploy production sau HTTPS (cookie chỉ gửi qua kết nối mã hoá)
COOKIE_SECURE=false

# --- OTP email (Gmail SMTP + App Password) ---
# Tạo App Password tại: Google Account > Security > 2-Step Verification > App Passwords
# (yêu cầu tài khoản Gmail đã bật xác minh 2 bước). SMTP_USER là địa chỉ Gmail gửi mail,
# SMTP_PASSWORD là App Password (KHÔNG phải mật khẩu Gmail thường).
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
